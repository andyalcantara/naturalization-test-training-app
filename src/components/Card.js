import React from 'react';
import { View, Text } from 'react-native';

const Card = (props) => {
    return (
        <View>
            <Text>Hello Card Functional Component</Text>
        </View>
    );
}

export default Card;